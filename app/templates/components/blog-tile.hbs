<li>{{blog.author}}'s {{blog.title}} on {{blog.date}}
    {{#if isImageShowing }}
      <p><img src={{blog.image}} alt={{blog.type}} {{action 'imageHide'}}></p>
     {{else}}
       <button {{action 'imageShow'}}>Show image</button>
    {{/if}}
</li>
